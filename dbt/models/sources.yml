name: "balta_task"
version: "1.0.0"
profile: "balta_task"
model-paths: ["models"]
seed-paths: ["data"]

models:
  balta_task:
    +materialized: table
    bronze:
      +schema: staging_balta
    silver:
      +schema: dwh
    gold:
      +schema: dwh

seeds:
  balta_task:
    +schema: staging_balta
